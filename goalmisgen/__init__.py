"""Goal misgeneralisation in multi-objective maze agents."""

from goalmisgen import quiet

# Applied on import, on the same principle as ``configs.compat``: every entry
# point in the project imports something from this package, and there is no
# other single place that catches all of them.
quiet.install()
